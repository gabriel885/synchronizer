package synchronizer.verticles.p2p;

import io.vertx.core.AbstractVerticle;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import synchronizer.app.StorageApplication;

// responsible for uploading file data
public class UploadFileDataVerticle extends NetworkVerticle {

    // logger
    private static final Logger logger = LogManager.getLogger(UploadFileDataVerticle.class);


    @Override
    public void start(){

    }

    @Override
    public void stop(){

    }
}
