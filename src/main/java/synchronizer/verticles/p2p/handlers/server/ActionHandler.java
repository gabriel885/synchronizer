package synchronizer.verticles.p2p.handlers.server;

import io.vertx.core.Handler;

import java.net.Socket;

public class ActionHandler implements Handler<Socket> {
    @Override
    public void handle(Socket event) {
        //
    }
}
